// Runtime config for static frontend.
// Set `window.API_BASE` to your backend origin so client code can call the API.
// Update this file when deploying the static site, or let the backend serve /config.js dynamically.
window.API_BASE = 'https://xa-poloan.onrender.com';
